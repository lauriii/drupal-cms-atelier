# The footer credit is a deliberate override of Nebula's own default, which
# reads "Built with Acquia Source". `tests/regenerate-components.sh` re-exports
# this region from a site the CLI has pushed to, so it will revert the text —
# re-apply the change after regenerating.
langcode: en
status: true
dependencies:
  config:
    - canvas.component.js.footer
  theme:
    - nebula_theme
id: nebula_theme.footer
theme: nebula_theme
region: footer
component_tree:
  bf4d99cc-0951-4b69-a323-04f21c7d53ce:
    uuid: bf4d99cc-0951-4b69-a323-04f21c7d53ce
    component_id: js.footer
    component_version: a9d389fa9057d81f
    inputs:
      text: 'Powered by Drupal CMS'
